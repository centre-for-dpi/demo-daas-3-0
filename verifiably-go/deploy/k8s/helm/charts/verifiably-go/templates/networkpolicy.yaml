{{- if .Values.app.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "verifiably-go.fullname" . }}
  labels: {{- include "verifiably-go.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels: {{- include "verifiably-go.selectorLabels" . | nindent 6 }}
  policyTypes: ["Ingress", "Egress"]
  ingress:
    - from:
        {{- toYaml .Values.app.networkPolicy.allowFrom | nindent 8 }}
      ports:
        - protocol: TCP
          port: {{ .Values.app.service.port }}
  egress:
    # DNS
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels: { k8s-app: kube-dns }
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # Outbound HTTPS — DPG calls, OCSP/CRL, OIDC discovery, etc.
    - to: [{ ipBlock: { cidr: 0.0.0.0/0 } }]
      ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 80
{{- end }}
