apiVersion: v2
name: keycloak
description: Keycloak IdP — wraps Bitnami's keycloak chart with our values + realm import.
type: application
version: 0.1.0
appVersion: "25.0"

dependencies:
  - name: keycloak
    version: "22.2.6"
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: keycloak.enabled
