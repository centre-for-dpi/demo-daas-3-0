apiVersion: v1
kind: Service
metadata:
  name: {{ include "walt-issuer.fullname" . }}
  labels: {{- include "walt-issuer.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.issuer.service.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "walt-issuer.selectorLabels" . | nindent 4 }}
