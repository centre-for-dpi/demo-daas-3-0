{{- if .Values.verifier.ingress.enabled }}
{{- $host := include "walt-verifier.ingressHost" . }}
{{- if not $host }}
{{- fail "verifier.ingress.host or verifier.baseUrl or global.domain must be set" }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "walt-verifier.fullname" . }}
  labels: {{- include "walt-verifier.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.verifier.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- if .Values.global.clusterIssuer }}
    cert-manager.io/cluster-issuer: {{ .Values.global.clusterIssuer | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.verifier.ingress.className }}
  {{- if .Values.verifier.ingress.tls }}
  tls:
    - hosts: [{{ $host | quote }}]
      secretName: {{ include "walt-verifier.fullname" . }}-tls
  {{- end }}
  rules:
    - host: {{ $host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "walt-verifier.fullname" . }}
                port: { name: http }
{{- end }}
